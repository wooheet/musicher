<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kitri.bigdataShop.comment">
 
    
    
  <insert id="insert" parameterType="Map"> 
      insert into pro_comment values(#{prd_no },'test',#{pro_comment },sysdate)   
    </insert>
	

    
    <select id="list" resultType="commentresult"  >
       select * from comment_result 
    </select>
    
   
    
    
</mapper>